# Credenciais do Grafana (bootstrap do admin no primeiro boot).
# Copie para .env e ajuste se quiser. Padrões definidos no docker-compose.yml.
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=atlas
