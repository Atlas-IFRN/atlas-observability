# Datasource provisionado automaticamente no boot do Grafana.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: atlas-prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 15s
